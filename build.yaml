builders:
  autoStr:
    import: "package:dacros/builder.dart"
    builder_factories: [ "autoStrBuilder" ]
    build_extensions: { ".dart": [ "auto_str.g.part" ] }
    auto_apply: root_package
    build_to: cache
    applies_builders: [ "source_gen|combining_builder" ]